# Out-of-band operator tooling (NOT imported by main.py / bot.universe runtime).
# Making ``tools`` a regular package so the fixtures-only tests can import the
# generation/validation modules deterministically.
